node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# runtime shared database (created by the server/PHP)
berry_db.json
**/berry_db.json

# runtime member accounts file (holds emails + password hashes — never commit)
berry_users.json
**/berry_users.json
